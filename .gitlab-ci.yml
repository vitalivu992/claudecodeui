stages:
  - build
  - sync

variables:
  GIT_STRATEGY: clone
  GIT_DEPTH: 0

# Build stage - build the application with Docker
build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t $CI_PROJECT_NAME:$CI_COMMIT_SHA .
    - docker create --name extract $CI_PROJECT_NAME:$CI_COMMIT_SHA
    - docker cp extract:/app/dist ./dist
    - docker rm extract
  artifacts:
    paths:
      - dist/
    expire_in: 1 hour
  only:
    - main
    - master

github:
  stage: sync
  image: alpine:latest
  before_script:
    - apk add --no-cache git openssh-client
    - eval $(ssh-agent -s)
    - echo "$GITHUB_DEPLOY_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan github.com >> ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
  script:
    - git remote add github git@github.com:vitalivu992/claudecodeui.git || git remote set-url github git@github.com:vitalivu992/claudecodeui.git
    - git fetch github
    - git checkout $CI_COMMIT_BRANCH
    - git pull --rebase origin $CI_COMMIT_BRANCH
    - git push github $CI_COMMIT_BRANCH
  dependencies:
    - build
  only:
    - main
    - master